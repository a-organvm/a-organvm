# excretory--metabolize.yaml — formation gate contract

identity:
  name: excretory--metabolize
  mechanism: excretory
  verb: metabolize
  signal_inputs: [WASTE, VALIDATION, STATE]
  signal_outputs: [STATE, TRACE]

sources:
  - repo: meta-organvm/organvm-engine
    modules: [src/organvm_engine/irf/cribrum.py]
    note: "Isotope detection and resolution heuristics"

gate:
  - id: G1
    check: ISOTOPE_CONSUMPTION
    condition: "Resolve immune--score.yaml isotopes (vox internal vs publica)"
    status: PENDING

  - id: G2
    check: LINEAGE_TRANSFER
    condition: "Ensure unique logic from consumed functions is preserved in the survivor (A9)"
    status: PENDING

dna:
  - "assessment pending — source module irf/cribrum.py does not exist in engine; no code to absorb"

defect:
  - "Source module irf/cribrum.py is declared but absent — only irf/parser.py and irf/query.py exist"
  - "Isotope consumption logic (G1) has no implementation anywhere in the engine"
  - "Lineage transfer (G2) has no implementation — no function merger or logic preservation code exists"
  - "Contract references immune--score.yaml isotopes but no immune mechanism contracts exist yet"

state: CALLING
