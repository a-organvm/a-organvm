# excretory--metabolize.yaml — formation gate contract

identity:
  name: excretory--metabolize
  mechanism: excretory
  verb: metabolize
  signal_inputs: [WASTE, VALIDATION, STATE]
  signal_outputs: [STATE, TRACE]

sources:
  - repo: meta-organvm/organvm-engine
    modules: [src/organvm_engine/irf/cribrum.py]
    note: "Isotope detection and resolution heuristics"

gate:
  - id: G1
    check: ISOTOPE_CONSUMPTION
    condition: "Resolve immune--score.yaml isotopes (vox internal vs publica)"
    status: PENDING

  - id: G2
    check: LINEAGE_TRANSFER
    condition: "Ensure unique logic from consumed functions is preserved in the survivor (A9)"
    status: PENDING

state: CALLING
