# reproductive--generate.yaml — formation gate contract

identity:
  name: reproductive--generate
  mechanism: reproductive
  verb: generate
  signal_inputs: [KNOWLEDGE, CONTRACT, RULE]
  signal_outputs: [KNOWLEDGE, CONTRACT, TEACHING]

sources:
  - repo: meta-organvm/organvm-engine
    modules: [contextmd/]
    lines: 1650
    note: shared with integumentary — molt decides which cocoon absorbs
  - repo: meta-organvm/organvm-ontologia
    modules: [bootstrap/]
    lines: 213

gate:
  - id: G1
    check: CONTEXTMD_PLACEMENT
    condition: "contextmd lives here (generation) OR integumentary (presentation) — not both"
    status: PENDING

  - id: G2
    check: TESTS_PASS
    condition: "contextmd + bootstrap tests pass"
    status: PENDING

dna:
  - Contextmd generator produces per-repo CLAUDE.md/GEMINI.md/AGENTS.md with 15+ template sections (1,670 lines, 4 test files)
  - Template-driven generation from registry + seed data — not string concatenation but structured section assembly
  - Surfaces sync module for cross-repo context file deployment across 127 repos
  - Ontologia bootstrap (213 lines) — entity seeding from registry for identity initialization

defect:
  - Placement ambiguity (G1 PENDING) — contextmd could live in reproductive (generation) OR integumentary (presentation), not both
  - Bootstrap module (213 lines) is minimal — may be a one-shot script disguised as a module
  - Content module referenced in contract sources but not listed in engine CLAUDE.md architecture table — orphan or undocumented
  - Test coverage (4 files for 1,670 lines) is thin for code that auto-generates context across the entire workspace

state: CALLING
