# circulatory--contribute.yaml — formation gate contract

identity:
  name: circulatory--contribute
  mechanism: circulatory
  verb: contribute
  signal_inputs: [KNOWLEDGE, DIRECTIVE]
  signal_outputs: [TRACE, REPORT, SOURCE]

sources:
  - repo: organvm-iv-taxis/orchestration-start-here
    modules: [contrib_engine/]
    lines: 2800
    note: contribution scanning, campaign management, outreach

gate:
  - id: G1
    check: ISOTOPES_DISSOLVED
    condition: "scripts/validate-deps.py, calculate-metrics.py, organ-audit.py, validate-all-seeds.py, generate-seed-from-registry.py all dissolved — functions absorbed by engine cocoons"
    status: PASS
    resolved: 2026-03-30
    commit: 085f9bb

  - id: G2
    check: REGISTRY_IMPORT_CANONICAL
    condition: "contrib_engine imports registry from skeletal--define, not hardcoded paths"
    status: PASS
    resolved: 2026-03-30
    commit: 085f9bb

  - id: G3
    check: TESTS_PASS
    condition: "contrib_engine tests pass after isotope dissolution"
    status: PASS
    resolved: 2026-03-30
    evidence: "150 passed in 0.50s"

dna:
  - Signal scanner discovers and ranks open-source contribution targets from application-pipeline + GitHub API
  - Campaign sequencer with prescriptive action queue, completion tracking, YAML persistence
  - Capabilities matching (match_capabilities) against ORGANVM skill surface — symbiotic targeting
  - GitHub client integration (forks, stars, issue search) for real network intelligence
  - 150 tests passing, isotopes dissolved, registry import canonical — all 3 gates PASS

defect:
  - Hardcoded path to application-pipeline (Path.home() / "Workspace" / "4444J99" / "application-pipeline") — breaks on any other machine
  - Campaign generation returns existing campaign if non-empty — no merge/update logic, only create-or-return
  - Outreach protocol exists as markdown (outreach_protocol.md) not code — the actual contribution workflow is manual
  - inbound-opportunity-protocol.zip is a compressed file inside a Python package — dead artifact

state: CONVERGING
