# respiratory--ingest.yaml — formation gate contract

identity:
  name: respiratory--ingest
  mechanism: respiratory
  verb: ingest
  signal_inputs: [SOURCE, MIGRATION]
  signal_outputs: [SOURCE, KNOWLEDGE]

sources:
  - repo: meta-organvm/alchemia-ingestvm
    modules: [src/alchemia/]
    lines: 3053

gate:
  - id: G1
    check: ISOTOPES_DISSOLVED
    condition: "absorb/registry_loader.py dissolved — imports from skeletal--define"
    status: PASS
    resolved: 2026-03-30
    commit: e800061

  - id: G2
    check: ORGAN_MAP_DISSOLVED
    condition: "synthesize.py::ORGAN_MAP dissolved — imports from skeletal--define"
    status: PASS
    resolved: 2026-03-30
    commit: e800061

  - id: G3
    check: TESTS_PASS
    condition: "alchemia test suite passes after isotope dissolution"
    status: PASS
    resolved: 2026-03-30
    evidence: "136 passed in 0.13s"

dna:
  - alchemia intake→absorb→alchemize pipeline — 3-phase material processing
  - taste.yaml cascades — aesthetic nervous system (style governance per organ)
  - capture command — URL/note/screenshot quick-capture to intake
  - Google Docs OAuth integration for gdocs source channel

defect:
  - absorb/registry_loader.py was isotope of engine registry — DISSOLVED (G1 PASS)
  - synthesize.py::ORGAN_MAP was isotope of organ_config — DISSOLVED (G2 PASS)
  - gdocs-auth OAuth flow incomplete — token refresh untested in production

state: CONVERGING
