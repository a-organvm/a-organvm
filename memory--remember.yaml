# memory--remember.yaml — formation gate contract

identity:
  name: memory--remember
  mechanism: memory
  verb: remember
  signal_inputs: [TRACE, ARCHIVE, KNOWLEDGE, REPORT, SYNTHESIS]
  signal_outputs: [ARCHIVE, KNOWLEDGE, TEACHING]

sources:
  - repo: meta-organvm/organvm-engine
    modules: [events/, fossil/, ledger/, testament/]
    lines: 8300
  - repo: meta-organvm/organvm-ontologia
    modules: [events/, state/]
    lines: 1150
  - repo: meta-organvm/organvm-corpvs-testamentvm
    modules: [docs/]
    lines: 0
    note: 400K+ words of constitutional corpus — docs, not code
  - repo: meta-organvm/praxis-perpetua
    modules: [docs/]
    lines: 0
    note: process governance SOPs — docs, not code

gate:
  - id: G1
    check: RESERVOIR_LAW
    condition: "memory formation does NOT emit RULE or KNOWLEDGE as canonical authority"
    status: PENDING

  - id: G2
    check: CORPUS_SCRIPTS_DISSOLVED
    condition: "corpvs-testamentvm/scripts/ (13,900 lines) dissolved — functions absorbed by other cocoons"
    status: PASS
    resolved: 2026-03-30
    commit: 018874f
    evidence: "19 scripts dissolved, all pass py_compile"

  - id: G3
    check: GRANULARITY
    condition: "may split: memory--remember (fossil/events), memory--witness (testament), memory--record (corpus)"
    status: PENDING

  - id: G4
    check: TESTS_PASS
    condition: "events + fossil + ledger + testament + ontologia events/state tests pass"
    status: PENDING

dna:
  - Fossil module (2,549 lines, 20 test files) — living stratigraphy with Jungian archetype classification, epoch chronicles, drift detection, witness hooks
  - Ledger module (1,046 lines, 18 test files) — Merkle chain, rotation, tiered digest — cryptographic integrity for event history
  - Events/spine (498 lines) — event bus backbone for system-wide state changes
  - Testament module (1,751 lines, 8 test files) — aesthetic catalog, manifest, network, pipeline, sources — the system rendering its own density
  - Ontologia events/state modules for UID-based entity lifecycle tracking

defect:
  - Corpus scripts already dissolved (G2 PASS) but 400K+ words of docs have no code interface — memory formation has no retrieval mechanism for the corpus it remembers
  - May need to split into 3 cocoons (remember/witness/record) — current shape is too broad for single formation
  - Reservoir law (G1) not yet enforced — risk of memory emitting RULE or KNOWLEDGE as canonical authority
  - Ontologia state/events integration depth is assessment pending — 1,150 lines across two modules may be thin glue

state: CALLING
expected_splits: [memory--remember, memory--witness, memory--record]
